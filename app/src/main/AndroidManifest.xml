<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vorane.gymassistant">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:label="@string/app_name"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Home" />
        <activity android:name=".Exercises" />
        <activity android:name=".SpecificExercise" />
        <activity android:name=".DisplayExercise" />
        <activity android:name=".Diet" />
        <activity android:name=".Routines" />
        <activity android:name=".AddRutine" />
        <activity
            android:name=".AddWorkout"
            android:theme="@style/Dialog" />
        <activity android:name=".NewRoutine" />
        <activity android:name=".Sups" />
        <activity android:name=".Timer" />
        <activity android:name=".timeralert" />
        <activity android:name=".calculator"></activity>
    </application>

</manifest>